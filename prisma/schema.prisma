generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_DATABASE_URL")
}

enum CompanyStatus {
  ACTIVE
  SUSPENDED
  TRIALING
  ARCHIVED

  @@map("company_status")
}

enum PlatformUserStatus {
  ACTIVE
  DISABLED

  @@map("platform_user_status")
}

enum UserStatus {
  INVITED
  ACTIVE
  DISABLED

  @@map("user_status")
}

enum AuthSubjectType {
  USER
  PLATFORM_USER

  @@map("auth_subject_type")
}

enum AuthSessionStatus {
  ACTIVE
  REVOKED
  EXPIRED

  @@map("auth_session_status")
}

enum MfaFactorType {
  TOTP
  WEBAUTHN
  RECOVERY_CODE

  @@map("mfa_factor_type")
}

enum MfaFactorStatus {
  PENDING
  ACTIVE
  DISABLED

  @@map("mfa_factor_status")
}

enum AuditActorType {
  PLATFORM_USER
  USER
  CANDIDATE_SESSION
  SYSTEM

  @@map("audit_actor_type")
}

enum AuditRiskLevel {
  LOW
  MEDIUM
  HIGH
  CRITICAL

  @@map("audit_risk_level")
}

enum IdempotencyStatus {
  PROCESSING
  COMPLETED
  FAILED
  EXPIRED

  @@map("idempotency_status")
}

enum SupportAccessStatus {
  REQUESTED
  ACTIVE
  EXPIRED
  REVOKED
  DENIED

  @@map("support_access_status")
}

enum SubscriptionStatus {
  TRIALING
  ACTIVE
  PAST_DUE
  SUSPENDED
  CANCELLED

  @@map("subscription_status")
}

enum LegalHoldStatus {
  ACTIVE
  RELEASED

  @@map("legal_hold_status")
}

enum PrivacyRequestType {
  ACCESS
  DELETION
  ANONYMIZATION
  EXPORT
  CORRECTION

  @@map("privacy_request_type")
}

enum PrivacyRequestStatus {
  RECEIVED
  VERIFYING
  PROCESSING
  COMPLETED
  DENIED

  @@map("privacy_request_status")
}

enum ExportRequestType {
  CANDIDATE_REPORT
  ROLE_SUMMARY
  AUDIT_EXPORT
  TENANT_EXPORT
  COMPLIANCE_EXPORT

  @@map("export_request_type")
}

enum ExportRequestStatus {
  PENDING
  PROCESSING
  READY
  FAILED
  EXPIRED

  @@map("export_request_status")
}

enum DepartmentStatus {
  ACTIVE
  ARCHIVED

  @@map("department_status")
}

enum TeamStatus {
  ACTIVE
  ARCHIVED

  @@map("team_status")
}

enum TeamMemberRole {
  LEAD
  MEMBER

  @@map("team_member_role")
}

enum LocationStatus {
  ACTIVE
  ARCHIVED

  @@map("location_status")
}

enum LocationMode {
  ONSITE
  HYBRID
  REMOTE

  @@map("location_mode")
}

enum HiringPipelineStatus {
  ACTIVE
  ARCHIVED

  @@map("hiring_pipeline_status")
}

enum PipelineStageStatus {
  ACTIVE
  ARCHIVED

  @@map("pipeline_stage_status")
}

enum PipelineStageCategory {
  APPLICATION_REVIEW
  SCREEN
  INTERVIEW
  OFFER
  HIRED
  REJECTED

  @@map("pipeline_stage_category")
}

enum JobStatus {
  DRAFT
  OPEN
  PAUSED
  CLOSED
  ARCHIVED

  @@map("job_status")
}

enum JobTemplateStatus {
  ACTIVE
  ARCHIVED

  @@map("job_template_status")
}

enum EmploymentType {
  FULL_TIME
  PART_TIME
  CONTRACT
  TEMPORARY
  INTERNSHIP

  @@map("employment_type")
}

enum WorkplaceType {
  ONSITE
  HYBRID
  REMOTE

  @@map("workplace_type")
}

enum SeniorityLevel {
  ENTRY
  MID
  SENIOR
  STAFF
  EXECUTIVE

  @@map("seniority_level")
}

enum InterviewPlanStatus {
  DRAFT
  ACTIVE
  ARCHIVED

  @@map("interview_plan_status")
}

enum InterviewPlanVersionStatus {
  DRAFT
  PUBLISHED
  RETIRED

  @@map("interview_plan_version_status")
}

enum CandidateStatus {
  ACTIVE
  ARCHIVED
  MERGED

  @@map("candidate_status")
}

enum CandidateSourceType {
  MANUAL
  REFERRAL
  IMPORT
  APPLICATION
  INVITATION

  @@map("candidate_source_type")
}

enum ApplicationStatus {
  NEW
  IN_REVIEW
  INTERVIEW
  OFFER
  HIRED
  REJECTED
  WITHDRAWN
  ARCHIVED

  @@map("application_status")
}

enum CandidateDocumentType {
  RESUME
  COVER_LETTER
  PORTFOLIO
  OTHER

  @@map("candidate_document_type")
}

enum CandidateDocumentStatus {
  ACTIVE
  DELETED

  @@map("candidate_document_status")
}

enum CandidateTagStatus {
  ACTIVE
  ARCHIVED

  @@map("candidate_tag_status")
}

enum CandidateNoteVisibility {
  INTERNAL
  PRIVATE

  @@map("candidate_note_visibility")
}

enum CandidateNoteStatus {
  ACTIVE
  DELETED

  @@map("candidate_note_status")
}

enum InvitationStatus {
  DRAFT
  QUEUED
  SENT
  OPENED
  ACCEPTED
  EXPIRED
  CANCELLED

  @@map("invitation_status")
}

enum InterviewSessionStatus {
  NOT_STARTED
  READY_CHECK
  READY
  IN_PROGRESS
  INTERRUPTED
  UPLOAD_RECOVERY
  COMPLETED
  PROCESSING
  CANCELLED
  EXPIRED
  WITHDRAWN

  @@map("interview_session_status")
}

enum InterviewQuestionKind {
  OPENING
  MAIN
  CLOSING
  FOLLOW_UP

  @@map("interview_question_kind")
}

enum InterviewQuestionStateStatus {
  PENDING
  ACTIVE
  ANSWERED
  SKIPPED

  @@map("interview_question_state_status")
}

enum InterviewTurnSpeaker {
  INTERVIEWER
  CANDIDATE
  SYSTEM

  @@map("interview_turn_speaker")
}

enum InterviewTurnStatus {
  STARTED
  COMPLETED
  RETRY_REQUESTED
  SUPERSEDED
  CANCELLED

  @@map("interview_turn_status")
}

enum InterviewTurnMediaStatus {
  PENDING
  UPLOADED
  VERIFIED
  FAILED

  @@map("interview_turn_media_status")
}

enum InterviewActivityType {
  HEARTBEAT
  CONNECTION_LOST
  CONNECTION_RESTORED
  INTERRUPTED
  RESUMED
  UPLOAD_RECOVERY_STARTED

  @@map("interview_activity_type")
}

enum InterviewRecoveryType {
  SESSION
  INTERRUPTION
  UPLOAD

  @@map("interview_recovery_type")
}

enum InterviewRecoveryStatus {
  OPEN
  RESOLVED
  EXPIRED

  @@map("interview_recovery_status")
}

enum CandidateSessionStatus {
  ACTIVE
  EXPIRED
  REVOKED
  COMPLETED

  @@map("candidate_session_status")
}

enum CandidateTokenAttemptOutcome {
  SUCCESS
  MALFORMED
  NOT_FOUND
  EXPIRED
  REVOKED
  CONSUMED
  COMPLETED
  RATE_LIMITED

  @@map("candidate_token_attempt_outcome")
}

enum CandidateConsentType {
  INTERVIEW_PARTICIPATION
  CAMERA_USE
  MICROPHONE_USE
  WEBCAM_SNAPSHOT
  FUTURE_AUDIO_VIDEO_RECORDING
  FUTURE_BROWSER_MONITORING
  PRIVACY_NOTICE
  DATA_PROCESSING_RETENTION

  @@map("candidate_consent_type")
}

enum ReadinessCheckType {
  CAMERA
  MICROPHONE
  BROWSER
  SECURE_CONTEXT
  MEDIA_DEVICES
  NETWORK
  DEVICE
  SCREEN_SIZE
  AUDIO_OUTPUT

  @@map("readiness_check_type")
}

enum ReadinessCheckStatus {
  PASS
  WARNING
  FAIL

  @@map("readiness_check_status")
}

enum IdentityVerificationProvider {
  INTERNAL_SELF_ATTESTATION

  @@map("identity_verification_provider")
}

enum IdentityVerificationStatus {
  PENDING
  SELF_ATTESTED
  SNAPSHOT_SUBMITTED
  NEEDS_REVIEW

  @@map("identity_verification_status")
}

enum AccommodationRequestType {
  WEBCAM_ALTERNATIVE
  TIME_EXTENSION
  ACCESSIBILITY_SUPPORT
  OTHER

  @@map("accommodation_request_type")
}

enum CandidateRequestStatus {
  OPEN
  ACKNOWLEDGED
  RESOLVED
  CANCELLED

  @@map("candidate_request_status")
}

enum CandidateSupportCategory {
  TECHNICAL
  ACCESSIBILITY
  SCHEDULING
  PRIVACY
  OTHER

  @@map("candidate_support_category")
}

enum ScheduleEventType {
  INTERVIEW
  DEADLINE
  REMINDER

  @@map("schedule_event_type")
}

enum ScheduleEventStatus {
  SCHEDULED
  CANCELLED
  COMPLETED

  @@map("schedule_event_status")
}

enum CalendarProvider {
  INTERNAL
  GOOGLE
  MICROSOFT

  @@map("calendar_provider")
}

enum ScheduleParticipantType {
  CANDIDATE
  USER
  EXTERNAL

  @@map("schedule_participant_type")
}

enum ScheduleResponseStatus {
  NEEDS_ACTION
  ACCEPTED
  DECLINED
  TENTATIVE

  @@map("schedule_response_status")
}

enum NotificationIntentType {
  INVITATION_CREATED
  INVITATION_REMINDER
  SCHEDULE_CREATED
  SCHEDULE_UPDATED
  CANDIDATE_ACCOMMODATION_REQUESTED
  CANDIDATE_SUPPORT_REQUESTED
  CANDIDATE_WITHDRAWN

  @@map("notification_intent_type")
}

enum NotificationChannel {
  EMAIL

  @@map("notification_channel")
}

enum NotificationIntentStatus {
  PENDING
  CANCELLED
  DISPATCHED
  FAILED

  @@map("notification_intent_status")
}

enum EmailProvider {
  SMTP
  PREVIEW

  @@map("email_provider")
}

enum SmtpProfileStatus {
  ACTIVE
  DISABLED
  ARCHIVED

  @@map("smtp_profile_status")
}

enum SenderDomainStatus {
  PENDING
  VERIFIED
  FAILED
  REVOKED

  @@map("sender_domain_status")
}

enum EmailTemplateKey {
  INTERVIEW_INVITATION
  INTERVIEW_REMINDER
  INVITATION_EXPIRED
  EMAIL_VERIFICATION
  PASSWORD_RESET

  @@map("email_template_key")
}

enum EmailTemplateStatus {
  DRAFT
  PUBLISHED
  ARCHIVED

  @@map("email_template_status")
}

enum EmailDeliveryStatus {
  PENDING
  QUEUED
  SENDING
  SENT
  DELIVERED
  DEFERRED
  BOUNCED
  COMPLAINED
  FAILED
  CANCELLED

  @@map("email_delivery_status")
}

enum EmailDeliveryAttemptStatus {
  SENDING
  SENT
  DEFERRED
  FAILED

  @@map("email_delivery_attempt_status")
}

enum EmailEventType {
  DELIVERED
  DEFERRED
  BOUNCED
  COMPLAINED
  FAILED

  @@map("email_event_type")
}

enum WorkflowStatus {
  PENDING
  RUNNING
  COMPLETED
  FAILED
  CANCELLED
  PARTIALLY_COMPLETED

  @@map("workflow_status")
}

enum WorkflowStepStatus {
  PENDING
  QUEUED
  RUNNING
  SUCCEEDED
  RETRY_SCHEDULED
  FAILED
  CANCELLED
  SKIPPED

  @@map("workflow_step_status")
}

enum WorkflowFailureKind {
  RETRYABLE
  TERMINAL

  @@map("workflow_failure_kind")
}

enum WorkflowDeadLetterStatus {
  OPEN
  REPLAYED
  CANCELLED
  RESOLVED

  @@map("workflow_dead_letter_status")
}

enum MediaOwnerType {
  CANDIDATE
  CANDIDATE_SESSION
  INVITATION
  INTERVIEW_SESSION
  IDENTITY_VERIFICATION
  EXPORT

  @@map("media_owner_type")
}

enum MediaPurpose {
  IDENTITY_SNAPSHOT
  INTERVIEW_RECORDING
  REPORT_EXPORT
  GENERAL_ATTACHMENT

  @@map("media_purpose")
}

enum MediaUploadStatus {
  PENDING
  AUTHORIZED
  UPLOADING
  COMPLETED
  ABORTED
  EXPIRED
  FAILED

  @@map("media_upload_status")
}

enum MediaProcessingStatus {
  PENDING
  READY
  PROCESSING
  FAILED
  QUARANTINED
  DELETED

  @@map("media_processing_status")
}

enum MediaRetentionClass {
  IDENTITY_VERIFICATION
  RECORDING
  EXPORT
  OPERATIONAL

  @@map("media_retention_class")
}

enum MediaUploadKind {
  SINGLE_PART
  MULTIPART

  @@map("media_upload_kind")
}

model Company {
  id            String        @id @default(cuid())
  name          String
  slug          String        @unique
  status        CompanyStatus @default(TRIALING)
  primaryDomain String?       @map("primary_domain")
  logoUrl       String?       @map("logo_url")
  createdAt     DateTime      @default(now()) @map("created_at")
  updatedAt     DateTime      @updatedAt @map("updated_at")
  deletedAt     DateTime?     @map("deleted_at")

  users                   User[]
  roles                   Role[]
  auditEvents             AuditEvent[]
  idempotencyKeys         IdempotencyKey[]
  settings                CompanySettings?
  subscription            Subscription?
  entitlements            Entitlement[]
  usageCounters           UsageCounter[]
  supportAccess           SupportAccessSession[]
  authCredentials         AuthCredential[]
  authSessions            AuthSession[]
  passwordResetTokens     PasswordResetToken[]
  emailVerificationTokens EmailVerificationToken[]
  mfaFactors              MfaFactor[]
  legalHolds              LegalHold[]
  privacyRequests         PrivacyRequest[]
  exportRequests          ExportRequest[]
  departments             Department[]
  teams                   Team[]
  teamMembers             TeamMember[]
  locations               Location[]
  hiringPipelines         HiringPipeline[]
  pipelineStages          PipelineStage[]
  jobs                    Job[]
  jobTemplates            JobTemplate[]
  interviewPlans          InterviewPlan[]
  interviewPlanVersions   InterviewPlanVersion[]
  candidates              Candidate[]
  candidateApplications   CandidateApplication[]
  candidateDocuments      CandidateDocument[]
  candidateTags           CandidateTag[]
  candidateTagAssignments CandidateTagAssignment[]
  candidateNotes          CandidateNote[]
  candidateMergeEvents    CandidateMergeEvent[]
  candidateInvitations    CandidateInvitation[]
  interviewSessions       InterviewSession[]
  candidateSessions       CandidateSession[]
  candidateSessionContinuations CandidateSessionContinuation[]
  candidateTokenAttempts  CandidateTokenAttempt[]
  candidateConsentRecords CandidateConsentRecord[]
  readinessChecks         ReadinessCheck[]
  identityVerifications   IdentityVerification[]
  webcamSnapshots         WebcamSnapshot[]
  accommodationRequests   AccommodationRequest[]
  candidateSupportRequests CandidateSupportRequest[]
  candidateWithdrawals    CandidateWithdrawal[]
  scheduleEvents          ScheduleEvent[]
  scheduleParticipants    ScheduleParticipant[]
  notificationIntents     NotificationIntent[]
  emailSettings           EmailSettings?
  smtpProfiles            SmtpProfile[]
  verifiedSenderDomains   VerifiedSenderDomain[]
  emailTemplates          EmailTemplate[]
  emailDeliveries         EmailDelivery[]
  emailDeliveryAttempts   EmailDeliveryAttempt[]
  emailEvents             EmailEvent[]
  processingWorkflows     ProcessingWorkflow[]
  processingWorkflowSteps ProcessingWorkflowStep[]
  workflowStepAttempts    WorkflowStepAttempt[]
  workflowDeadLetterJobs  WorkflowDeadLetterJob[]
  mediaObjects            MediaObject[]
  mediaUploadSessions     MediaUploadSession[]
  mediaUploadParts        MediaUploadPart[]
  interviewQuestionStates InterviewQuestionState[]
  interviewTurns          InterviewTurn[]
  interviewTurnMedia      InterviewTurnMedia[]
  interviewActivityEvents InterviewActivityEvent[]
  interviewRecoveryCheckpoints InterviewRecoveryCheckpoint[]
  interviewStateHistory   InterviewStateHistory[]

  @@index([status])
  @@map("companies")
}

model PlatformUser {
  id        String             @id @default(cuid())
  email     String             @unique
  name      String
  status    PlatformUserStatus @default(ACTIVE)
  createdAt DateTime           @default(now()) @map("created_at")
  updatedAt DateTime           @updatedAt @map("updated_at")

  supportAccessSessions         SupportAccessSession[]   @relation("SupportAccessPlatformUser")
  approvedSupportAccessSessions SupportAccessSession[]   @relation("SupportAccessApprover")
  credentials                   AuthCredential[]
  sessions                      AuthSession[]
  passwordResetTokens           PasswordResetToken[]
  emailVerificationTokens       EmailVerificationToken[]
  mfaFactors                    MfaFactor[]

  @@index([status])
  @@map("platform_users")
}

model User {
  id          String     @id @default(cuid())
  companyId   String     @map("company_id")
  email       String
  name        String
  status      UserStatus @default(INVITED)
  lastLoginAt DateTime?  @map("last_login_at")
  createdAt   DateTime   @default(now()) @map("created_at")
  updatedAt   DateTime   @updatedAt @map("updated_at")
  deletedAt   DateTime?  @map("deleted_at")

  company                 Company                  @relation(fields: [companyId], references: [id], onDelete: Restrict)
  roles                   UserRole[]
  legalHoldsCreated       LegalHold[]              @relation("LegalHoldCreator")
  legalHoldsReleased      LegalHold[]              @relation("LegalHoldReleaser")
  exportRequests          ExportRequest[]
  teamMemberships         TeamMember[]
  candidateNotes          CandidateNote[]
  candidateMergeEvents    CandidateMergeEvent[]
  credentials             AuthCredential[]
  sessions                AuthSession[]
  passwordResetTokens     PasswordResetToken[]
  emailVerificationTokens EmailVerificationToken[]
  mfaFactors              MfaFactor[]

  @@unique([companyId, id])
  @@unique([companyId, email])
  @@index([companyId, status])
  @@map("users")
}

model Role {
  id          String   @id @default(cuid())
  companyId   String   @map("company_id")
  name        String
  key         String
  description String?
  isSystem    Boolean  @default(false) @map("is_system")
  createdAt   DateTime @default(now()) @map("created_at")
  updatedAt   DateTime @updatedAt @map("updated_at")

  company     Company          @relation(fields: [companyId], references: [id], onDelete: Restrict)
  permissions RolePermission[]
  users       UserRole[]

  @@unique([companyId, id])
  @@unique([companyId, key])
  @@index([companyId])
  @@map("roles")
}

model Permission {
  id          String   @id @default(cuid())
  key         String   @unique
  description String
  createdAt   DateTime @default(now()) @map("created_at")

  roles RolePermission[]

  @@map("permissions")
}

model RolePermission {
  roleId       String @map("role_id")
  permissionId String @map("permission_id")

  role       Role       @relation(fields: [roleId], references: [id], onDelete: Cascade)
  permission Permission @relation(fields: [permissionId], references: [id], onDelete: Cascade)

  @@id([roleId, permissionId])
  @@map("role_permissions")
}

model UserRole {
  userId    String @map("user_id")
  roleId    String @map("role_id")
  companyId String @map("company_id")

  user User @relation(fields: [companyId, userId], references: [companyId, id], onDelete: Cascade)
  role Role @relation(fields: [companyId, roleId], references: [companyId, id], onDelete: Cascade)

  @@id([userId, roleId])
  @@index([companyId])
  @@map("user_roles")
}

model AuthCredential {
  id                String          @id @default(cuid())
  subjectType       AuthSubjectType @map("subject_type")
  companyId         String?         @map("company_id")
  userId            String?         @map("user_id")
  platformUserId    String?         @map("platform_user_id")
  passwordHash      String          @map("password_hash")
  emailVerifiedAt   DateTime?       @map("email_verified_at")
  passwordUpdatedAt DateTime        @default(now()) @map("password_updated_at")
  createdAt         DateTime        @default(now()) @map("created_at")
  updatedAt         DateTime        @updatedAt @map("updated_at")

  company      Company?      @relation(fields: [companyId], references: [id], onDelete: Restrict)
  user         User?         @relation(fields: [companyId, userId], references: [companyId, id], onDelete: Cascade)
  platformUser PlatformUser? @relation(fields: [platformUserId], references: [id], onDelete: Cascade)

  @@unique([companyId, userId])
  @@unique([platformUserId])
  @@index([companyId])
  @@map("auth_credentials")
}

model AuthSession {
  id               String            @id @default(cuid())
  subjectType      AuthSubjectType   @map("subject_type")
  companyId        String?           @map("company_id")
  userId           String?           @map("user_id")
  platformUserId   String?           @map("platform_user_id")
  sessionTokenHash String            @unique @map("session_token_hash")
  refreshTokenHash String            @unique @map("refresh_token_hash")
  csrfTokenHash    String            @map("csrf_token_hash")
  status           AuthSessionStatus @default(ACTIVE)
  ipAddress        String?           @map("ip_address")
  userAgent        String?           @map("user_agent")
  lastSeenAt       DateTime          @default(now()) @map("last_seen_at")
  expiresAt        DateTime          @map("expires_at")
  refreshExpiresAt DateTime          @map("refresh_expires_at")
  revokedAt        DateTime?         @map("revoked_at")
  createdAt        DateTime          @default(now()) @map("created_at")
  updatedAt        DateTime          @updatedAt @map("updated_at")

  company      Company?      @relation(fields: [companyId], references: [id], onDelete: Restrict)
  user         User?         @relation(fields: [companyId, userId], references: [companyId, id], onDelete: Cascade)
  platformUser PlatformUser? @relation(fields: [platformUserId], references: [id], onDelete: Cascade)

  @@unique([companyId, id])
  @@index([companyId, userId, status])
  @@index([platformUserId, status])
  @@index([expiresAt])
  @@index([refreshExpiresAt])
  @@map("auth_sessions")
}

model PasswordResetToken {
  id             String          @id @default(cuid())
  subjectType    AuthSubjectType @map("subject_type")
  companyId      String?         @map("company_id")
  userId         String?         @map("user_id")
  platformUserId String?         @map("platform_user_id")
  tokenHash      String          @unique @map("token_hash")
  expiresAt      DateTime        @map("expires_at")
  usedAt         DateTime?       @map("used_at")
  createdAt      DateTime        @default(now()) @map("created_at")

  company      Company?      @relation(fields: [companyId], references: [id], onDelete: Restrict)
  user         User?         @relation(fields: [companyId, userId], references: [companyId, id], onDelete: Cascade)
  platformUser PlatformUser? @relation(fields: [platformUserId], references: [id], onDelete: Cascade)

  @@index([companyId, userId])
  @@index([platformUserId])
  @@index([expiresAt])
  @@map("password_reset_tokens")
}

model EmailVerificationToken {
  id             String          @id @default(cuid())
  subjectType    AuthSubjectType @map("subject_type")
  companyId      String?         @map("company_id")
  userId         String?         @map("user_id")
  platformUserId String?         @map("platform_user_id")
  tokenHash      String          @unique @map("token_hash")
  expiresAt      DateTime        @map("expires_at")
  verifiedAt     DateTime?       @map("verified_at")
  createdAt      DateTime        @default(now()) @map("created_at")

  company      Company?      @relation(fields: [companyId], references: [id], onDelete: Restrict)
  user         User?         @relation(fields: [companyId, userId], references: [companyId, id], onDelete: Cascade)
  platformUser PlatformUser? @relation(fields: [platformUserId], references: [id], onDelete: Cascade)

  @@index([companyId, userId])
  @@index([platformUserId])
  @@index([expiresAt])
  @@map("email_verification_tokens")
}

model MfaFactor {
  id             String          @id @default(cuid())
  subjectType    AuthSubjectType @map("subject_type")
  companyId      String?         @map("company_id")
  userId         String?         @map("user_id")
  platformUserId String?         @map("platform_user_id")
  type           MfaFactorType
  status         MfaFactorStatus @default(PENDING)
  secretRef      String?         @map("secret_ref")
  metadataJson   Json            @map("metadata_json")
  createdAt      DateTime        @default(now()) @map("created_at")
  updatedAt      DateTime        @updatedAt @map("updated_at")
  disabledAt     DateTime?       @map("disabled_at")

  company      Company?      @relation(fields: [companyId], references: [id], onDelete: Restrict)
  user         User?         @relation(fields: [companyId, userId], references: [companyId, id], onDelete: Cascade)
  platformUser PlatformUser? @relation(fields: [platformUserId], references: [id], onDelete: Cascade)

  @@unique([companyId, userId, type])
  @@unique([platformUserId, type])
  @@index([companyId, status])
  @@map("mfa_factors")
}

model AuditEvent {
  id                     String         @id @default(cuid())
  companyId              String?        @map("company_id")
  actorType              AuditActorType @map("actor_type")
  actorId                String?        @map("actor_id")
  requestId              String?        @map("request_id")
  correlationId          String?        @map("correlation_id")
  sessionId              String?        @map("session_id")
  supportAccessSessionId String?        @map("support_access_session_id")
  action                 String
  resourceType           String         @map("resource_type")
  resourceId             String?        @map("resource_id")
  reason                 String?
  riskLevel              AuditRiskLevel @default(LOW) @map("risk_level")
  beforeJson             Json?          @map("before_json")
  afterJson              Json?          @map("after_json")
  ipAddress              String?        @map("ip_address")
  userAgent              String?        @map("user_agent")
  metadataJson           Json?          @map("metadata_json")
  createdAt              DateTime       @default(now()) @map("created_at")

  company              Company?              @relation(fields: [companyId], references: [id], onDelete: Restrict)
  supportAccessSession SupportAccessSession? @relation(fields: [supportAccessSessionId], references: [id], onDelete: Restrict)

  @@index([companyId, createdAt])
  @@index([actorType, actorId])
  @@index([resourceType, resourceId])
  @@index([requestId])
  @@index([correlationId])
  @@index([supportAccessSessionId])
  @@map("audit_events")
}

model IdempotencyKey {
  id           String            @id @default(cuid())
  companyId    String?           @map("company_id")
  key          String
  scope        String
  requestHash  String            @map("request_hash")
  responseHash String?           @map("response_hash")
  status       IdempotencyStatus @default(PROCESSING)
  expiresAt    DateTime          @map("expires_at")
  createdAt    DateTime          @default(now()) @map("created_at")
  updatedAt    DateTime          @updatedAt @map("updated_at")

  company Company? @relation(fields: [companyId], references: [id], onDelete: Restrict)

  @@unique([scope, key])
  @@index([companyId])
  @@index([expiresAt])
  @@map("idempotency_keys")
}

model CompanySettings {
  id                  String   @id @default(cuid())
  companyId           String   @unique @map("company_id")
  brandingJson        Json     @map("branding_json")
  retentionPolicyJson Json     @map("retention_policy_json")
  emailSettingsJson   Json     @map("email_settings_json")
  featureFlagsJson    Json     @map("feature_flags_json")
  createdAt           DateTime @default(now()) @map("created_at")
  updatedAt           DateTime @updatedAt @map("updated_at")

  company Company @relation(fields: [companyId], references: [id], onDelete: Restrict)

  @@unique([companyId, id])
  @@map("company_settings")
}

model Subscription {
  id                     String             @id @default(cuid())
  companyId              String             @unique @map("company_id")
  status                 SubscriptionStatus @default(TRIALING)
  planKey                String             @map("plan_key")
  billingProvider        String?            @map("billing_provider")
  billingCustomerRef     String?            @map("billing_customer_ref")
  billingSubscriptionRef String?            @map("billing_subscription_ref")
  currentPeriodStart     DateTime?          @map("current_period_start")
  currentPeriodEnd       DateTime?          @map("current_period_end")
  usageJson              Json               @map("usage_json")
  createdAt              DateTime           @default(now()) @map("created_at")
  updatedAt              DateTime           @updatedAt @map("updated_at")

  company Company @relation(fields: [companyId], references: [id], onDelete: Restrict)

  @@unique([companyId, id])
  @@index([status])
  @@map("subscriptions")
}

model Entitlement {
  id             String   @id @default(cuid())
  companyId      String   @map("company_id")
  planKey        String   @map("plan_key")
  featureKey     String   @map("feature_key")
  limitValue     Int?     @map("limit_value")
  enabled        Boolean  @default(false)
  overrideReason String?  @map("override_reason")
  createdAt      DateTime @default(now()) @map("created_at")
  updatedAt      DateTime @updatedAt @map("updated_at")

  company Company @relation(fields: [companyId], references: [id], onDelete: Restrict)

  @@unique([companyId, id])
  @@unique([companyId, featureKey])
  @@index([companyId, planKey])
  @@map("entitlements")
}

model UsageCounter {
  id           String   @id @default(cuid())
  companyId    String   @map("company_id")
  periodStart  DateTime @map("period_start")
  periodEnd    DateTime @map("period_end")
  metricKey    String   @map("metric_key")
  count        Int      @default(0)
  metadataJson Json     @map("metadata_json")
  createdAt    DateTime @default(now()) @map("created_at")
  updatedAt    DateTime @updatedAt @map("updated_at")

  company Company @relation(fields: [companyId], references: [id], onDelete: Restrict)

  @@unique([companyId, id])
  @@unique([companyId, periodStart, periodEnd, metricKey])
  @@index([companyId, metricKey])
  @@map("usage_counters")
}

model SupportAccessSession {
  id                       String              @id @default(cuid())
  companyId                String              @map("company_id")
  platformUserId           String              @map("platform_user_id")
  status                   SupportAccessStatus @default(REQUESTED)
  reasonCode               String              @map("reason_code")
  reasonText               String              @map("reason_text")
  approvedByPlatformUserId String?             @map("approved_by_platform_user_id")
  startedAt                DateTime?           @map("started_at")
  expiresAt                DateTime            @map("expires_at")
  endedAt                  DateTime?           @map("ended_at")
  createdAt                DateTime            @default(now()) @map("created_at")
  updatedAt                DateTime            @updatedAt @map("updated_at")

  company                Company       @relation(fields: [companyId], references: [id], onDelete: Restrict)
  platformUser           PlatformUser  @relation("SupportAccessPlatformUser", fields: [platformUserId], references: [id], onDelete: Restrict)
  approvedByPlatformUser PlatformUser? @relation("SupportAccessApprover", fields: [approvedByPlatformUserId], references: [id], onDelete: Restrict)
  auditEvents            AuditEvent[]

  @@unique([companyId, id])
  @@index([companyId, status])
  @@index([platformUserId, status])
  @@index([expiresAt])
  @@map("support_access_sessions")
}

model LegalHold {
  id               String          @id @default(cuid())
  companyId        String          @map("company_id")
  name             String
  description      String?
  status           LegalHoldStatus @default(ACTIVE)
  createdByUserId  String          @map("created_by_user_id")
  releasedByUserId String?         @map("released_by_user_id")
  createdAt        DateTime        @default(now()) @map("created_at")
  releasedAt       DateTime?       @map("released_at")

  company        Company @relation(fields: [companyId], references: [id], onDelete: Restrict)
  createdByUser  User    @relation("LegalHoldCreator", fields: [companyId, createdByUserId], references: [companyId, id], onDelete: Restrict)
  releasedByUser User?   @relation("LegalHoldReleaser", fields: [companyId, releasedByUserId], references: [companyId, id], onDelete: Restrict)

  @@unique([companyId, id])
  @@index([companyId, status])
  @@map("legal_holds")
}

model PrivacyRequest {
  id             String               @id @default(cuid())
  companyId      String               @map("company_id")
  candidateId    String?              @map("candidate_id")
  type           PrivacyRequestType
  status         PrivacyRequestStatus @default(RECEIVED)
  requesterEmail String               @map("requester_email")
  reason         String?
  completedAt    DateTime?            @map("completed_at")
  createdAt      DateTime             @default(now()) @map("created_at")
  updatedAt      DateTime             @updatedAt @map("updated_at")

  company   Company    @relation(fields: [companyId], references: [id], onDelete: Restrict)
  candidate Candidate? @relation(fields: [companyId, candidateId], references: [companyId, id], onDelete: Restrict)

  @@unique([companyId, id])
  @@index([companyId, candidateId])
  @@index([companyId, status])
  @@map("privacy_requests")
}

model ExportRequest {
  id                String              @id @default(cuid())
  companyId         String              @map("company_id")
  requestedByUserId String              @map("requested_by_user_id")
  type              ExportRequestType
  status            ExportRequestStatus @default(PENDING)
  resourceType      String?             @map("resource_type")
  resourceId        String?             @map("resource_id")
  storageKey        String?             @map("storage_key")
  expiresAt         DateTime?           @map("expires_at")
  createdAt         DateTime            @default(now()) @map("created_at")
  updatedAt         DateTime            @updatedAt @map("updated_at")

  company         Company @relation(fields: [companyId], references: [id], onDelete: Restrict)
  requestedByUser User    @relation(fields: [companyId, requestedByUserId], references: [companyId, id], onDelete: Restrict)

  @@unique([companyId, id])
  @@index([companyId, status])
  @@index([companyId, requestedByUserId])
  @@map("export_requests")
}

model Department {
  id          String           @id @default(cuid())
  companyId   String           @map("company_id")
  name        String
  slug        String
  description String?
  status      DepartmentStatus @default(ACTIVE)
  createdAt   DateTime         @default(now()) @map("created_at")
  updatedAt   DateTime         @updatedAt @map("updated_at")
  deletedAt   DateTime?        @map("deleted_at")

  company Company @relation(fields: [companyId], references: [id], onDelete: Restrict)
  teams   Team[]
  jobs    Job[]

  @@unique([companyId, id])
  @@unique([companyId, slug])
  @@index([companyId, status])
  @@map("departments")
}

model Team {
  id           String     @id @default(cuid())
  companyId    String     @map("company_id")
  departmentId String?    @map("department_id")
  name         String
  slug         String
  description  String?
  status       TeamStatus @default(ACTIVE)
  createdAt    DateTime   @default(now()) @map("created_at")
  updatedAt    DateTime   @updatedAt @map("updated_at")
  deletedAt    DateTime?  @map("deleted_at")

  company    Company      @relation(fields: [companyId], references: [id], onDelete: Restrict)
  department Department?  @relation(fields: [companyId, departmentId], references: [companyId, id], onDelete: Restrict)
  members    TeamMember[]
  jobs       Job[]

  @@unique([companyId, id])
  @@unique([companyId, slug])
  @@index([companyId, departmentId])
  @@index([companyId, status])
  @@map("teams")
}

model TeamMember {
  id        String         @id @default(cuid())
  companyId String         @map("company_id")
  teamId    String         @map("team_id")
  userId    String         @map("user_id")
  role      TeamMemberRole @default(MEMBER)
  createdAt DateTime       @default(now()) @map("created_at")
  updatedAt DateTime       @updatedAt @map("updated_at")

  company Company @relation(fields: [companyId], references: [id], onDelete: Restrict)
  team    Team    @relation(fields: [companyId, teamId], references: [companyId, id], onDelete: Cascade)
  user    User    @relation(fields: [companyId, userId], references: [companyId, id], onDelete: Cascade)

  @@unique([companyId, id])
  @@unique([companyId, teamId, userId])
  @@index([companyId, userId])
  @@map("team_members")
}

model Location {
  id          String         @id @default(cuid())
  companyId   String         @map("company_id")
  name        String
  slug        String
  mode        LocationMode
  status      LocationStatus @default(ACTIVE)
  addressJson Json?          @map("address_json")
  timeZone    String?        @map("time_zone")
  createdAt   DateTime       @default(now()) @map("created_at")
  updatedAt   DateTime       @updatedAt @map("updated_at")
  deletedAt   DateTime?      @map("deleted_at")

  company Company @relation(fields: [companyId], references: [id], onDelete: Restrict)
  jobs    Job[]

  @@unique([companyId, id])
  @@unique([companyId, slug])
  @@index([companyId, status])
  @@index([companyId, mode])
  @@map("locations")
}

model HiringPipeline {
  id          String               @id @default(cuid())
  companyId   String               @map("company_id")
  name        String
  slug        String
  description String?
  status      HiringPipelineStatus @default(ACTIVE)
  createdAt   DateTime             @default(now()) @map("created_at")
  updatedAt   DateTime             @updatedAt @map("updated_at")
  deletedAt   DateTime?            @map("deleted_at")

  company      Company         @relation(fields: [companyId], references: [id], onDelete: Restrict)
  stages       PipelineStage[]
  jobs         Job[]
  jobTemplates JobTemplate[]

  @@unique([companyId, id])
  @@unique([companyId, slug])
  @@index([companyId, status])
  @@map("hiring_pipelines")
}

model PipelineStage {
  id         String                @id @default(cuid())
  companyId  String                @map("company_id")
  pipelineId String                @map("pipeline_id")
  name       String
  slug       String
  category   PipelineStageCategory
  position   Int
  isTerminal Boolean               @default(false) @map("is_terminal")
  status     PipelineStageStatus   @default(ACTIVE)
  createdAt  DateTime              @default(now()) @map("created_at")
  updatedAt  DateTime              @updatedAt @map("updated_at")
  deletedAt  DateTime?             @map("deleted_at")

  company      Company                @relation(fields: [companyId], references: [id], onDelete: Restrict)
  pipeline     HiringPipeline         @relation(fields: [companyId, pipelineId], references: [companyId, id], onDelete: Cascade)
  applications CandidateApplication[]

  @@unique([companyId, id])
  @@unique([companyId, pipelineId, slug])
  @@unique([companyId, pipelineId, position])
  @@index([companyId, pipelineId, status])
  @@map("pipeline_stages")
}

model Job {
  id               String         @id @default(cuid())
  companyId        String         @map("company_id")
  departmentId     String?        @map("department_id")
  teamId           String?        @map("team_id")
  locationId       String?        @map("location_id")
  pipelineId       String         @map("pipeline_id")
  title            String
  slug             String
  descriptionJson  Json           @map("description_json")
  requirementsJson Json           @map("requirements_json")
  employmentType   EmploymentType @map("employment_type")
  workplaceType    WorkplaceType  @map("workplace_type")
  seniorityLevel   SeniorityLevel @map("seniority_level")
  status           JobStatus      @default(DRAFT)
  openedAt         DateTime?      @map("opened_at")
  closedAt         DateTime?      @map("closed_at")
  createdAt        DateTime       @default(now()) @map("created_at")
  updatedAt        DateTime       @updatedAt @map("updated_at")
  deletedAt        DateTime?      @map("deleted_at")

  company      Company                @relation(fields: [companyId], references: [id], onDelete: Restrict)
  department   Department?            @relation(fields: [companyId, departmentId], references: [companyId, id], onDelete: Restrict)
  team         Team?                  @relation(fields: [companyId, teamId], references: [companyId, id], onDelete: Restrict)
  location     Location?              @relation(fields: [companyId, locationId], references: [companyId, id], onDelete: Restrict)
  pipeline     HiringPipeline         @relation(fields: [companyId, pipelineId], references: [companyId, id], onDelete: Restrict)
  plans        InterviewPlan[]
  applications CandidateApplication[]
  invitations  CandidateInvitation[]

  @@unique([companyId, id])
  @@unique([companyId, slug])
  @@index([companyId, status])
  @@index([companyId, pipelineId])
  @@index([companyId, departmentId])
  @@index([companyId, teamId])
  @@index([companyId, locationId])
  @@map("jobs")
}

model JobTemplate {
  id               String            @id @default(cuid())
  companyId        String            @map("company_id")
  pipelineId       String?           @map("pipeline_id")
  title            String
  slug             String
  descriptionJson  Json              @map("description_json")
  requirementsJson Json              @map("requirements_json")
  employmentType   EmploymentType?   @map("employment_type")
  workplaceType    WorkplaceType?    @map("workplace_type")
  seniorityLevel   SeniorityLevel?   @map("seniority_level")
  status           JobTemplateStatus @default(ACTIVE)
  createdAt        DateTime          @default(now()) @map("created_at")
  updatedAt        DateTime          @updatedAt @map("updated_at")
  deletedAt        DateTime?         @map("deleted_at")

  company  Company         @relation(fields: [companyId], references: [id], onDelete: Restrict)
  pipeline HiringPipeline? @relation(fields: [companyId, pipelineId], references: [companyId, id], onDelete: Restrict)
  plans    InterviewPlan[]

  @@unique([companyId, id])
  @@unique([companyId, slug])
  @@index([companyId, status])
  @@index([companyId, pipelineId])
  @@map("job_templates")
}

model InterviewPlan {
  id              String              @id @default(cuid())
  companyId       String              @map("company_id")
  jobId           String?             @map("job_id")
  jobTemplateId   String?             @map("job_template_id")
  name            String
  status          InterviewPlanStatus @default(DRAFT)
  activeVersionId String?             @map("active_version_id")
  createdAt       DateTime            @default(now()) @map("created_at")
  updatedAt       DateTime            @updatedAt @map("updated_at")
  deletedAt       DateTime?           @map("deleted_at")

  company     Company                @relation(fields: [companyId], references: [id], onDelete: Restrict)
  job         Job?                   @relation(fields: [companyId, jobId], references: [companyId, id], onDelete: Cascade)
  jobTemplate JobTemplate?           @relation(fields: [companyId, jobTemplateId], references: [companyId, id], onDelete: Cascade)
  versions    InterviewPlanVersion[]

  @@unique([companyId, id])
  @@index([companyId, jobId])
  @@index([companyId, jobTemplateId])
  @@index([companyId, status])
  @@map("interview_plans")
}

model InterviewPlanVersion {
  id                    String                     @id @default(cuid())
  companyId             String                     @map("company_id")
  interviewPlanId       String                     @map("interview_plan_id")
  versionNumber         Int                        @map("version_number")
  status                InterviewPlanVersionStatus @default(DRAFT)
  competencyJson        Json                       @map("competency_json")
  questionBlueprintJson Json                       @map("question_blueprint_json")
  durationMinutes       Int                        @map("duration_minutes")
  publishedAt           DateTime?                  @map("published_at")
  createdAt             DateTime                   @default(now()) @map("created_at")
  updatedAt             DateTime                   @updatedAt @map("updated_at")

  company           Company            @relation(fields: [companyId], references: [id], onDelete: Restrict)
  interviewPlan     InterviewPlan      @relation(fields: [companyId, interviewPlanId], references: [companyId, id], onDelete: Cascade)
  interviewSessions InterviewSession[]

  @@unique([companyId, id])
  @@unique([companyId, interviewPlanId, versionNumber])
  @@index([companyId, interviewPlanId, status])
  @@map("interview_plan_versions")
}

model Candidate {
  id              String              @id @default(cuid())
  companyId       String              @map("company_id")
  primaryEmail    String?             @map("primary_email")
  normalizedEmail String?             @map("normalized_email")
  fullName        String              @map("full_name")
  phone           String?
  sourceType      CandidateSourceType @default(MANUAL) @map("source_type")
  sourceLabel     String?             @map("source_label")
  status          CandidateStatus     @default(ACTIVE)
  profileJson     Json                @map("profile_json")
  mergedIntoId    String?             @map("merged_into_id")
  createdAt       DateTime            @default(now()) @map("created_at")
  updatedAt       DateTime            @updatedAt @map("updated_at")
  deletedAt       DateTime?           @map("deleted_at")

  company           Company                  @relation(fields: [companyId], references: [id], onDelete: Restrict)
  mergedInto        Candidate?               @relation("CandidateMergeTarget", fields: [companyId, mergedIntoId], references: [companyId, id], onDelete: Restrict)
  mergedCandidates  Candidate[]              @relation("CandidateMergeTarget")
  applications      CandidateApplication[]
  documents         CandidateDocument[]
  tagAssignments    CandidateTagAssignment[]
  notes             CandidateNote[]
  invitations       CandidateInvitation[]
  interviewSessions InterviewSession[]
  candidateSessions CandidateSession[]
  consentRecords    CandidateConsentRecord[]
  readinessChecks   ReadinessCheck[]
  identityVerifications IdentityVerification[]
  webcamSnapshots   WebcamSnapshot[]
  accommodationRequests AccommodationRequest[]
  supportRequests   CandidateSupportRequest[]
  withdrawals       CandidateWithdrawal[]
  sourceMergeEvents CandidateMergeEvent[]    @relation("CandidateMergeSource")
  targetMergeEvents CandidateMergeEvent[]    @relation("CandidateMergeTargetEvent")
  privacyRequests   PrivacyRequest[]

  @@unique([companyId, id])
  @@unique([companyId, normalizedEmail])
  @@index([companyId, status])
  @@index([companyId, fullName])
  @@map("candidates")
}

model CandidateApplication {
  id             String            @id @default(cuid())
  companyId      String            @map("company_id")
  candidateId    String            @map("candidate_id")
  jobId          String            @map("job_id")
  currentStageId String?           @map("current_stage_id")
  status         ApplicationStatus @default(NEW)
  appliedAt      DateTime          @default(now()) @map("applied_at")
  rejectedAt     DateTime?         @map("rejected_at")
  hiredAt        DateTime?         @map("hired_at")
  metadataJson   Json              @map("metadata_json")
  createdAt      DateTime          @default(now()) @map("created_at")
  updatedAt      DateTime          @updatedAt @map("updated_at")
  deletedAt      DateTime?         @map("deleted_at")

  company           Company               @relation(fields: [companyId], references: [id], onDelete: Restrict)
  candidate         Candidate             @relation(fields: [companyId, candidateId], references: [companyId, id], onDelete: Cascade)
  job               Job                   @relation(fields: [companyId, jobId], references: [companyId, id], onDelete: Restrict)
  currentStage      PipelineStage?        @relation(fields: [companyId, currentStageId], references: [companyId, id], onDelete: Restrict)
  invitations       CandidateInvitation[]
  interviewSessions InterviewSession[]

  @@unique([companyId, id])
  @@unique([companyId, candidateId, jobId])
  @@index([companyId, jobId, status])
  @@index([companyId, candidateId])
  @@index([companyId, currentStageId])
  @@map("candidate_applications")
}

model CandidateDocument {
  id             String                  @id @default(cuid())
  companyId      String                  @map("company_id")
  candidateId    String                  @map("candidate_id")
  type           CandidateDocumentType
  status         CandidateDocumentStatus @default(ACTIVE)
  fileName       String                  @map("file_name")
  contentType    String                  @map("content_type")
  storageKey     String                  @map("storage_key")
  sizeBytes      Int                     @map("size_bytes")
  checksumSha256 String                  @map("checksum_sha256")
  createdAt      DateTime                @default(now()) @map("created_at")
  updatedAt      DateTime                @updatedAt @map("updated_at")
  deletedAt      DateTime?               @map("deleted_at")

  company   Company   @relation(fields: [companyId], references: [id], onDelete: Restrict)
  candidate Candidate @relation(fields: [companyId, candidateId], references: [companyId, id], onDelete: Cascade)

  @@unique([companyId, id])
  @@unique([companyId, storageKey])
  @@index([companyId, candidateId, status])
  @@map("candidate_documents")
}

model CandidateTag {
  id        String             @id @default(cuid())
  companyId String             @map("company_id")
  name      String
  slug      String
  color     String?
  status    CandidateTagStatus @default(ACTIVE)
  createdAt DateTime           @default(now()) @map("created_at")
  updatedAt DateTime           @updatedAt @map("updated_at")
  deletedAt DateTime?          @map("deleted_at")

  company     Company                  @relation(fields: [companyId], references: [id], onDelete: Restrict)
  assignments CandidateTagAssignment[]

  @@unique([companyId, id])
  @@unique([companyId, slug])
  @@index([companyId, status])
  @@map("candidate_tags")
}

model CandidateTagAssignment {
  id          String   @id @default(cuid())
  companyId   String   @map("company_id")
  candidateId String   @map("candidate_id")
  tagId       String   @map("tag_id")
  createdAt   DateTime @default(now()) @map("created_at")

  company   Company      @relation(fields: [companyId], references: [id], onDelete: Restrict)
  candidate Candidate    @relation(fields: [companyId, candidateId], references: [companyId, id], onDelete: Cascade)
  tag       CandidateTag @relation(fields: [companyId, tagId], references: [companyId, id], onDelete: Cascade)

  @@unique([companyId, id])
  @@unique([companyId, candidateId, tagId])
  @@index([companyId, tagId])
  @@map("candidate_tag_assignments")
}

model CandidateNote {
  id           String                  @id @default(cuid())
  companyId    String                  @map("company_id")
  candidateId  String                  @map("candidate_id")
  authorUserId String                  @map("author_user_id")
  body         String
  visibility   CandidateNoteVisibility @default(INTERNAL)
  status       CandidateNoteStatus     @default(ACTIVE)
  createdAt    DateTime                @default(now()) @map("created_at")
  updatedAt    DateTime                @updatedAt @map("updated_at")
  deletedAt    DateTime?               @map("deleted_at")

  company   Company   @relation(fields: [companyId], references: [id], onDelete: Restrict)
  candidate Candidate @relation(fields: [companyId, candidateId], references: [companyId, id], onDelete: Cascade)
  author    User      @relation(fields: [companyId, authorUserId], references: [companyId, id], onDelete: Restrict)

  @@unique([companyId, id])
  @@index([companyId, candidateId, status])
  @@index([companyId, authorUserId])
  @@map("candidate_notes")
}

model CandidateMergeEvent {
  id                String   @id @default(cuid())
  companyId         String   @map("company_id")
  sourceCandidateId String   @map("source_candidate_id")
  targetCandidateId String   @map("target_candidate_id")
  mergedByUserId    String   @map("merged_by_user_id")
  reason            String
  metadataJson      Json     @map("metadata_json")
  createdAt         DateTime @default(now()) @map("created_at")

  company         Company   @relation(fields: [companyId], references: [id], onDelete: Restrict)
  sourceCandidate Candidate @relation("CandidateMergeSource", fields: [companyId, sourceCandidateId], references: [companyId, id], onDelete: Restrict)
  targetCandidate Candidate @relation("CandidateMergeTargetEvent", fields: [companyId, targetCandidateId], references: [companyId, id], onDelete: Restrict)
  mergedByUser    User      @relation(fields: [companyId, mergedByUserId], references: [companyId, id], onDelete: Restrict)

  @@unique([companyId, id])
  @@index([companyId, sourceCandidateId])
  @@index([companyId, targetCandidateId])
  @@map("candidate_merge_events")
}

model CandidateInvitation {
  id            String           @id @default(cuid())
  companyId     String           @map("company_id")
  candidateId   String           @map("candidate_id")
  applicationId String?          @map("application_id")
  jobId         String           @map("job_id")
  tokenHash     String           @unique @map("token_hash")
  email         String
  status        InvitationStatus @default(DRAFT)
  expiresAt     DateTime         @map("expires_at")
  sentAt        DateTime?        @map("sent_at")
  openedAt      DateTime?        @map("opened_at")
  acceptedAt    DateTime?        @map("accepted_at")
  tokenConsumedAt DateTime?      @map("token_consumed_at")
  tokenRevokedAt  DateTime?      @map("token_revoked_at")
  tokenRotatedAt  DateTime?      @map("token_rotated_at")
  resendCount     Int            @default(0) @map("resend_count")
  cancelledAt   DateTime?        @map("cancelled_at")
  createdAt     DateTime         @default(now()) @map("created_at")
  updatedAt     DateTime         @updatedAt @map("updated_at")

  company     Company               @relation(fields: [companyId], references: [id], onDelete: Restrict)
  candidate   Candidate             @relation(fields: [companyId, candidateId], references: [companyId, id], onDelete: Cascade)
  application CandidateApplication? @relation(fields: [companyId, applicationId], references: [companyId, id], onDelete: Cascade)
  job         Job                   @relation(fields: [companyId, jobId], references: [companyId, id], onDelete: Restrict)
  sessions    InterviewSession[]
  candidateSessions CandidateSession[]
  tokenAttempts CandidateTokenAttempt[]
  consentRecords CandidateConsentRecord[]
  readinessChecks ReadinessCheck[]
  identityVerifications IdentityVerification[]
  webcamSnapshots WebcamSnapshot[]
  accommodationRequests AccommodationRequest[]
  supportRequests CandidateSupportRequest[]
  withdrawals CandidateWithdrawal[]

  @@unique([companyId, id])
  @@index([companyId, candidateId])
  @@index([companyId, applicationId])
  @@index([companyId, status])
  @@index([companyId, tokenConsumedAt])
  @@index([expiresAt])
  @@map("candidate_invitations")
}

model InterviewSession {
  id                     String                 @id @default(cuid())
  companyId              String                 @map("company_id")
  candidateId            String                 @map("candidate_id")
  invitationId           String                 @map("invitation_id")
  applicationId          String?                @map("application_id")
  interviewPlanVersionId String?                @map("interview_plan_version_id")
  status                 InterviewSessionStatus @default(NOT_STARTED)
  startedAt              DateTime?              @map("started_at")
  interruptedAt          DateTime?              @map("interrupted_at")
  completedAt            DateTime?              @map("completed_at")
  lastActivityAt         DateTime?              @map("last_activity_at")
  durationSeconds        Int?                   @map("duration_seconds")
  currentQuestionSequence Int?                  @map("current_question_sequence")
  resumeAllowedUntil     DateTime?              @map("resume_allowed_until")
  processingWorkflowId   String?                @map("processing_workflow_id")
  planSnapshotJson       Json?                  @map("plan_snapshot_json")
  expiresAt              DateTime               @map("expires_at")
  metadataJson           Json                   @map("metadata_json")
  createdAt              DateTime               @default(now()) @map("created_at")
  updatedAt              DateTime               @updatedAt @map("updated_at")

  company              Company               @relation(fields: [companyId], references: [id], onDelete: Restrict)
  candidate            Candidate             @relation(fields: [companyId, candidateId], references: [companyId, id], onDelete: Cascade)
  invitation           CandidateInvitation   @relation(fields: [companyId, invitationId], references: [companyId, id], onDelete: Cascade)
  application          CandidateApplication? @relation(fields: [companyId, applicationId], references: [companyId, id], onDelete: Cascade)
  interviewPlanVersion InterviewPlanVersion? @relation(fields: [companyId, interviewPlanVersionId], references: [companyId, id], onDelete: Restrict)
  candidateSessions    CandidateSession[]
  consentRecords       CandidateConsentRecord[]
  readinessChecks      ReadinessCheck[]
  identityVerifications IdentityVerification[]
  webcamSnapshots      WebcamSnapshot[]
  interviewQuestionStates InterviewQuestionState[]
  interviewTurns         InterviewTurn[]
  interviewTurnMedia     InterviewTurnMedia[]
  activityEvents         InterviewActivityEvent[]
  recoveryCheckpoints    InterviewRecoveryCheckpoint[]
  stateHistory           InterviewStateHistory[]

  @@unique([companyId, id])
  @@unique([companyId, invitationId])
  @@index([companyId, candidateId])
  @@index([companyId, applicationId])
  @@index([companyId, processingWorkflowId])
  @@index([companyId, status])
  @@index([expiresAt])
  @@map("interview_sessions")
}

model InterviewQuestionState {
  id                 String                       @id @default(cuid())
  companyId          String                       @map("company_id")
  interviewSessionId String                       @map("interview_session_id")
  sequence           Int
  questionKey        String                       @map("question_key")
  kind               InterviewQuestionKind
  prompt             String
  required           Boolean                      @default(true)
  status             InterviewQuestionStateStatus @default(PENDING)
  startedAt          DateTime?                    @map("started_at")
  completedAt        DateTime?                    @map("completed_at")
  metadataJson       Json                         @map("metadata_json")
  createdAt          DateTime                     @default(now()) @map("created_at")
  updatedAt          DateTime                     @updatedAt @map("updated_at")

  company          Company          @relation(fields: [companyId], references: [id], onDelete: Restrict)
  interviewSession InterviewSession @relation(fields: [companyId, interviewSessionId], references: [companyId, id], onDelete: Cascade)
  turns            InterviewTurn[]

  @@unique([companyId, id])
  @@unique([companyId, interviewSessionId, sequence])
  @@unique([companyId, interviewSessionId, questionKey])
  @@index([companyId, interviewSessionId, status])
  @@map("interview_question_states")
}

model InterviewTurn {
  id                 String              @id @default(cuid())
  companyId          String              @map("company_id")
  interviewSessionId String              @map("interview_session_id")
  questionStateId    String              @map("question_state_id")
  sequence           Int
  attemptNumber      Int                 @default(1) @map("attempt_number")
  speaker            InterviewTurnSpeaker
  status             InterviewTurnStatus @default(STARTED)
  content            String?
  idempotencyKey     String              @map("idempotency_key")
  startedAt          DateTime            @map("started_at")
  endedAt            DateTime?           @map("ended_at")
  confirmedAt        DateTime?           @map("confirmed_at")
  metadataJson       Json                @map("metadata_json")
  createdAt          DateTime            @default(now()) @map("created_at")
  updatedAt          DateTime            @updatedAt @map("updated_at")

  company          Company                @relation(fields: [companyId], references: [id], onDelete: Restrict)
  interviewSession InterviewSession       @relation(fields: [companyId, interviewSessionId], references: [companyId, id], onDelete: Cascade)
  questionState    InterviewQuestionState @relation(fields: [companyId, questionStateId], references: [companyId, id], onDelete: Restrict)
  media            InterviewTurnMedia[]

  @@unique([companyId, id])
  @@unique([companyId, idempotencyKey])
  @@unique([companyId, interviewSessionId, sequence, attemptNumber])
  @@index([companyId, interviewSessionId, status])
  @@index([companyId, questionStateId])
  @@map("interview_turns")
}

model InterviewTurnMedia {
  id                 String                   @id @default(cuid())
  companyId          String                   @map("company_id")
  interviewSessionId String                   @map("interview_session_id")
  interviewTurnId    String                   @map("interview_turn_id")
  mediaObjectId      String                   @map("media_object_id")
  chunkSequence      Int                      @map("chunk_sequence")
  durationMs         Int?                     @map("duration_ms")
  status             InterviewTurnMediaStatus @default(PENDING)
  metadataJson       Json                     @map("metadata_json")
  createdAt          DateTime                 @default(now()) @map("created_at")
  updatedAt          DateTime                 @updatedAt @map("updated_at")

  company          Company          @relation(fields: [companyId], references: [id], onDelete: Restrict)
  interviewSession InterviewSession @relation(fields: [companyId, interviewSessionId], references: [companyId, id], onDelete: Cascade)
  interviewTurn    InterviewTurn    @relation(fields: [companyId, interviewTurnId], references: [companyId, id], onDelete: Cascade)
  mediaObject      MediaObject      @relation(fields: [companyId, mediaObjectId], references: [companyId, id], onDelete: Restrict)

  @@unique([companyId, id])
  @@unique([companyId, interviewTurnId, chunkSequence])
  @@unique([companyId, interviewTurnId, mediaObjectId])
  @@index([companyId, interviewSessionId, status])
  @@index([companyId, mediaObjectId])
  @@map("interview_turn_media")
}

model InterviewActivityEvent {
  id                 String                @id @default(cuid())
  companyId          String                @map("company_id")
  interviewSessionId String                @map("interview_session_id")
  candidateSessionId String?               @map("candidate_session_id")
  type               InterviewActivityType
  occurredAt         DateTime              @default(now()) @map("occurred_at")
  metadataJson       Json                  @map("metadata_json")
  createdAt          DateTime              @default(now()) @map("created_at")

  company          Company           @relation(fields: [companyId], references: [id], onDelete: Restrict)
  interviewSession InterviewSession  @relation(fields: [companyId, interviewSessionId], references: [companyId, id], onDelete: Cascade)
  candidateSession CandidateSession? @relation(fields: [companyId, candidateSessionId], references: [companyId, id], onDelete: Restrict)

  @@unique([companyId, id])
  @@index([companyId, interviewSessionId, occurredAt])
  @@index([companyId, type, occurredAt])
  @@map("interview_activity_events")
}

model InterviewRecoveryCheckpoint {
  id                 String                  @id @default(cuid())
  companyId          String                  @map("company_id")
  interviewSessionId String                  @map("interview_session_id")
  candidateSessionId String?                 @map("candidate_session_id")
  type               InterviewRecoveryType
  status             InterviewRecoveryStatus @default(OPEN)
  checkpointJson     Json                    @map("checkpoint_json")
  expiresAt          DateTime                @map("expires_at")
  resolvedAt         DateTime?               @map("resolved_at")
  createdAt          DateTime                @default(now()) @map("created_at")
  updatedAt          DateTime                @updatedAt @map("updated_at")

  company          Company           @relation(fields: [companyId], references: [id], onDelete: Restrict)
  interviewSession InterviewSession  @relation(fields: [companyId, interviewSessionId], references: [companyId, id], onDelete: Cascade)
  candidateSession CandidateSession? @relation(fields: [companyId, candidateSessionId], references: [companyId, id], onDelete: Restrict)

  @@unique([companyId, id])
  @@index([companyId, interviewSessionId, status])
  @@index([companyId, status, expiresAt])
  @@map("interview_recovery_checkpoints")
}

model InterviewStateHistory {
  id                 String                  @id @default(cuid())
  companyId          String                  @map("company_id")
  interviewSessionId String                  @map("interview_session_id")
  fromStatus         InterviewSessionStatus? @map("from_status")
  toStatus           InterviewSessionStatus  @map("to_status")
  reason             String?
  metadataJson       Json                    @map("metadata_json")
  createdAt          DateTime                @default(now()) @map("created_at")

  company          Company          @relation(fields: [companyId], references: [id], onDelete: Restrict)
  interviewSession InterviewSession @relation(fields: [companyId, interviewSessionId], references: [companyId, id], onDelete: Cascade)

  @@unique([companyId, id])
  @@index([companyId, interviewSessionId, createdAt])
  @@index([companyId, toStatus, createdAt])
  @@map("interview_state_history")
}

model CandidateSession {
  id                 String                 @id @default(cuid())
  companyId          String                 @map("company_id")
  candidateId        String                 @map("candidate_id")
  invitationId       String                 @map("invitation_id")
  interviewSessionId String?                @map("interview_session_id")
  sessionTokenHash   String                 @unique @map("session_token_hash")
  csrfTokenHash      String                 @map("csrf_token_hash")
  activeLockKey      String?                @map("active_lock_key")
  status             CandidateSessionStatus @default(ACTIVE)
  createdFromIpHash  String?                @map("created_from_ip_hash")
  createdFromUserAgent String?              @map("created_from_user_agent")
  lastSeenAt         DateTime               @default(now()) @map("last_seen_at")
  expiresAt          DateTime               @map("expires_at")
  revokedAt          DateTime?              @map("revoked_at")
  completedAt        DateTime?              @map("completed_at")
  metadataJson       Json                   @map("metadata_json")
  createdAt          DateTime               @default(now()) @map("created_at")
  updatedAt          DateTime               @updatedAt @map("updated_at")

  company          Company             @relation(fields: [companyId], references: [id], onDelete: Restrict)
  candidate        Candidate           @relation(fields: [companyId, candidateId], references: [companyId, id], onDelete: Cascade)
  invitation       CandidateInvitation @relation(fields: [companyId, invitationId], references: [companyId, id], onDelete: Cascade)
  interviewSession InterviewSession?   @relation(fields: [companyId, interviewSessionId], references: [companyId, id], onDelete: Restrict)
  continuations    CandidateSessionContinuation[]
  consentRecords   CandidateConsentRecord[]
  readinessChecks  ReadinessCheck[]
  identityVerifications IdentityVerification[]
  webcamSnapshots  WebcamSnapshot[]
  accommodationRequests AccommodationRequest[]
  supportRequests  CandidateSupportRequest[]
  withdrawals      CandidateWithdrawal[]
  interviewActivityEvents InterviewActivityEvent[]
  interviewRecoveryCheckpoints InterviewRecoveryCheckpoint[]

  @@unique([companyId, id])
  @@unique([companyId, activeLockKey])
  @@index([companyId, invitationId, status])
  @@index([companyId, candidateId])
  @@index([expiresAt])
  @@map("candidate_sessions")
}

model CandidateSessionContinuation {
  id                String   @id @default(cuid())
  companyId         String   @map("company_id")
  candidateSessionId String  @map("candidate_session_id")
  resumeTokenHash   String   @unique @map("resume_token_hash")
  consumedAt        DateTime? @map("consumed_at")
  expiresAt         DateTime @map("expires_at")
  createdAt         DateTime @default(now()) @map("created_at")

  company          Company          @relation(fields: [companyId], references: [id], onDelete: Restrict)
  candidateSession CandidateSession @relation(fields: [companyId, candidateSessionId], references: [companyId, id], onDelete: Cascade)

  @@unique([companyId, id])
  @@index([companyId, candidateSessionId])
  @@index([expiresAt])
  @@map("candidate_session_continuations")
}

model CandidateTokenAttempt {
  id              String                       @id @default(cuid())
  companyId       String?                      @map("company_id")
  invitationId    String?                      @map("invitation_id")
  tokenHashPrefix String?                      @map("token_hash_prefix")
  ipAddressHash   String?                      @map("ip_address_hash")
  outcome         CandidateTokenAttemptOutcome
  attemptedAt     DateTime                     @default(now()) @map("attempted_at")
  metadataJson    Json                         @map("metadata_json")

  company    Company?             @relation(fields: [companyId], references: [id], onDelete: Restrict)
  invitation CandidateInvitation? @relation(fields: [companyId, invitationId], references: [companyId, id], onDelete: Cascade)

  @@unique([companyId, id])
  @@index([companyId, invitationId, attemptedAt])
  @@index([ipAddressHash, attemptedAt])
  @@index([tokenHashPrefix, attemptedAt])
  @@map("candidate_token_attempts")
}

model CandidateConsentRecord {
  id                 String               @id @default(cuid())
  companyId          String               @map("company_id")
  candidateId        String               @map("candidate_id")
  invitationId       String               @map("invitation_id")
  candidateSessionId String               @map("candidate_session_id")
  interviewSessionId String?              @map("interview_session_id")
  type               CandidateConsentType
  consentVersion     String               @map("consent_version")
  policyVersion      String               @map("policy_version")
  accepted           Boolean
  acceptedAt         DateTime?            @map("accepted_at")
  deniedAt           DateTime?            @map("denied_at")
  ipAddressHash      String?              @map("ip_address_hash")
  userAgent          String?              @map("user_agent")
  metadataJson       Json                 @map("metadata_json")
  createdAt          DateTime             @default(now()) @map("created_at")

  company          Company              @relation(fields: [companyId], references: [id], onDelete: Restrict)
  candidate        Candidate            @relation(fields: [companyId, candidateId], references: [companyId, id], onDelete: Cascade)
  invitation       CandidateInvitation  @relation(fields: [companyId, invitationId], references: [companyId, id], onDelete: Cascade)
  candidateSession CandidateSession     @relation(fields: [companyId, candidateSessionId], references: [companyId, id], onDelete: Cascade)
  interviewSession InterviewSession?    @relation(fields: [companyId, interviewSessionId], references: [companyId, id], onDelete: Restrict)

  @@unique([companyId, id])
  @@index([companyId, candidateSessionId, type])
  @@index([companyId, invitationId])
  @@map("candidate_consent_records")
}

model ReadinessCheck {
  id                 String               @id @default(cuid())
  companyId          String               @map("company_id")
  candidateId        String               @map("candidate_id")
  invitationId       String               @map("invitation_id")
  candidateSessionId String               @map("candidate_session_id")
  interviewSessionId String?              @map("interview_session_id")
  type               ReadinessCheckType
  status             ReadinessCheckStatus
  detailsJson        Json                 @map("details_json")
  checkedAt          DateTime             @map("checked_at")
  createdAt          DateTime             @default(now()) @map("created_at")

  company          Company             @relation(fields: [companyId], references: [id], onDelete: Restrict)
  candidate        Candidate           @relation(fields: [companyId, candidateId], references: [companyId, id], onDelete: Cascade)
  invitation       CandidateInvitation @relation(fields: [companyId, invitationId], references: [companyId, id], onDelete: Cascade)
  candidateSession CandidateSession    @relation(fields: [companyId, candidateSessionId], references: [companyId, id], onDelete: Cascade)
  interviewSession InterviewSession?   @relation(fields: [companyId, interviewSessionId], references: [companyId, id], onDelete: Restrict)

  @@unique([companyId, id])
  @@index([companyId, candidateSessionId, type, checkedAt])
  @@index([companyId, invitationId])
  @@map("readiness_checks")
}

model IdentityVerification {
  id                 String                       @id @default(cuid())
  companyId          String                       @map("company_id")
  candidateId        String                       @map("candidate_id")
  invitationId       String                       @map("invitation_id")
  candidateSessionId String                       @map("candidate_session_id")
  interviewSessionId String?                      @map("interview_session_id")
  provider           IdentityVerificationProvider @default(INTERNAL_SELF_ATTESTATION)
  status             IdentityVerificationStatus   @default(PENDING)
  selfAttestedName   String                       @map("self_attested_name")
  confirmedName      String?                      @map("confirmed_name")
  consentedAt        DateTime?                    @map("consented_at")
  metadataJson       Json                         @map("metadata_json")
  createdAt          DateTime                     @default(now()) @map("created_at")
  updatedAt          DateTime                     @updatedAt @map("updated_at")

  company          Company             @relation(fields: [companyId], references: [id], onDelete: Restrict)
  candidate        Candidate           @relation(fields: [companyId, candidateId], references: [companyId, id], onDelete: Cascade)
  invitation       CandidateInvitation @relation(fields: [companyId, invitationId], references: [companyId, id], onDelete: Cascade)
  candidateSession CandidateSession    @relation(fields: [companyId, candidateSessionId], references: [companyId, id], onDelete: Cascade)
  interviewSession InterviewSession?   @relation(fields: [companyId, interviewSessionId], references: [companyId, id], onDelete: Restrict)
  snapshots        WebcamSnapshot[]

  @@unique([companyId, id])
  @@index([companyId, candidateSessionId])
  @@index([companyId, candidateId, status])
  @@map("identity_verifications")
}

model WebcamSnapshot {
  id                     String   @id @default(cuid())
  companyId              String   @map("company_id")
  candidateId            String   @map("candidate_id")
  invitationId           String   @map("invitation_id")
  candidateSessionId     String   @map("candidate_session_id")
  interviewSessionId     String?  @map("interview_session_id")
  identityVerificationId String?  @map("identity_verification_id")
  storageRef             String   @map("storage_ref")
  contentType            String   @map("content_type")
  sizeBytes              Int      @map("size_bytes")
  checksumSha256         String   @map("checksum_sha256")
  capturedAt             DateTime @map("captured_at")
  metadataJson           Json     @map("metadata_json")
  createdAt              DateTime @default(now()) @map("created_at")

  company              Company               @relation(fields: [companyId], references: [id], onDelete: Restrict)
  candidate            Candidate             @relation(fields: [companyId, candidateId], references: [companyId, id], onDelete: Cascade)
  invitation           CandidateInvitation   @relation(fields: [companyId, invitationId], references: [companyId, id], onDelete: Cascade)
  candidateSession     CandidateSession      @relation(fields: [companyId, candidateSessionId], references: [companyId, id], onDelete: Cascade)
  interviewSession     InterviewSession?     @relation(fields: [companyId, interviewSessionId], references: [companyId, id], onDelete: Restrict)
  identityVerification IdentityVerification? @relation(fields: [companyId, identityVerificationId], references: [companyId, id], onDelete: Restrict)

  @@unique([companyId, id])
  @@index([companyId, candidateSessionId])
  @@index([companyId, identityVerificationId])
  @@map("webcam_snapshots")
}

model AccommodationRequest {
  id                 String                   @id @default(cuid())
  companyId          String                   @map("company_id")
  candidateId        String                   @map("candidate_id")
  invitationId       String                   @map("invitation_id")
  candidateSessionId String                   @map("candidate_session_id")
  type               AccommodationRequestType
  status             CandidateRequestStatus   @default(OPEN)
  contactEmail       String                   @map("contact_email")
  message            String
  metadataJson       Json                     @map("metadata_json")
  createdAt          DateTime                 @default(now()) @map("created_at")
  updatedAt          DateTime                 @updatedAt @map("updated_at")
  resolvedAt         DateTime?                @map("resolved_at")

  company          Company             @relation(fields: [companyId], references: [id], onDelete: Restrict)
  candidate        Candidate           @relation(fields: [companyId, candidateId], references: [companyId, id], onDelete: Cascade)
  invitation       CandidateInvitation @relation(fields: [companyId, invitationId], references: [companyId, id], onDelete: Cascade)
  candidateSession CandidateSession    @relation(fields: [companyId, candidateSessionId], references: [companyId, id], onDelete: Cascade)

  @@unique([companyId, id])
  @@index([companyId, invitationId, status])
  @@index([companyId, candidateSessionId])
  @@map("accommodation_requests")
}

model CandidateSupportRequest {
  id                 String                   @id @default(cuid())
  companyId          String                   @map("company_id")
  candidateId        String                   @map("candidate_id")
  invitationId       String                   @map("invitation_id")
  candidateSessionId String                   @map("candidate_session_id")
  category           CandidateSupportCategory
  status             CandidateRequestStatus   @default(OPEN)
  contactEmail       String                   @map("contact_email")
  message            String
  metadataJson       Json                     @map("metadata_json")
  createdAt          DateTime                 @default(now()) @map("created_at")
  updatedAt          DateTime                 @updatedAt @map("updated_at")
  resolvedAt         DateTime?                @map("resolved_at")

  company          Company             @relation(fields: [companyId], references: [id], onDelete: Restrict)
  candidate        Candidate           @relation(fields: [companyId, candidateId], references: [companyId, id], onDelete: Cascade)
  invitation       CandidateInvitation @relation(fields: [companyId, invitationId], references: [companyId, id], onDelete: Cascade)
  candidateSession CandidateSession    @relation(fields: [companyId, candidateSessionId], references: [companyId, id], onDelete: Cascade)

  @@unique([companyId, id])
  @@index([companyId, invitationId, status])
  @@index([companyId, candidateSessionId])
  @@map("candidate_support_requests")
}

model CandidateWithdrawal {
  id                 String                 @id @default(cuid())
  companyId          String                 @map("company_id")
  candidateId        String                 @map("candidate_id")
  invitationId       String                 @map("invitation_id")
  candidateSessionId String                 @map("candidate_session_id")
  reason             String?
  confirmedAt        DateTime               @map("confirmed_at")
  metadataJson       Json                   @map("metadata_json")
  createdAt          DateTime               @default(now()) @map("created_at")

  company          Company             @relation(fields: [companyId], references: [id], onDelete: Restrict)
  candidate        Candidate           @relation(fields: [companyId, candidateId], references: [companyId, id], onDelete: Cascade)
  invitation       CandidateInvitation @relation(fields: [companyId, invitationId], references: [companyId, id], onDelete: Cascade)
  candidateSession CandidateSession    @relation(fields: [companyId, candidateSessionId], references: [companyId, id], onDelete: Cascade)

  @@unique([companyId, id])
  @@unique([companyId, invitationId])
  @@index([companyId, candidateSessionId])
  @@map("candidate_withdrawals")
}

model ScheduleEvent {
  id                 String              @id @default(cuid())
  companyId          String              @map("company_id")
  type               ScheduleEventType
  status             ScheduleEventStatus @default(SCHEDULED)
  title              String
  description        String?
  startsAt           DateTime            @map("starts_at")
  endsAt             DateTime            @map("ends_at")
  timeZone           String              @map("time_zone")
  provider           CalendarProvider    @default(INTERNAL)
  providerEventRef   String?             @map("provider_event_ref")
  targetResourceType String?             @map("target_resource_type")
  targetResourceId   String?             @map("target_resource_id")
  metadataJson       Json                @map("metadata_json")
  createdAt          DateTime            @default(now()) @map("created_at")
  updatedAt          DateTime            @updatedAt @map("updated_at")
  cancelledAt        DateTime?           @map("cancelled_at")

  company      Company               @relation(fields: [companyId], references: [id], onDelete: Restrict)
  participants ScheduleParticipant[]

  @@unique([companyId, id])
  @@index([companyId, status, startsAt])
  @@index([companyId, targetResourceType, targetResourceId])
  @@map("schedule_events")
}

model ScheduleParticipant {
  id              String                  @id @default(cuid())
  companyId       String                  @map("company_id")
  scheduleEventId String                  @map("schedule_event_id")
  type            ScheduleParticipantType
  displayName     String                  @map("display_name")
  email           String?
  userId          String?                 @map("user_id")
  candidateId     String?                 @map("candidate_id")
  responseStatus  ScheduleResponseStatus  @default(NEEDS_ACTION) @map("response_status")
  createdAt       DateTime                @default(now()) @map("created_at")
  updatedAt       DateTime                @updatedAt @map("updated_at")

  company       Company       @relation(fields: [companyId], references: [id], onDelete: Restrict)
  scheduleEvent ScheduleEvent @relation(fields: [companyId, scheduleEventId], references: [companyId, id], onDelete: Cascade)

  @@unique([companyId, id])
  @@index([companyId, scheduleEventId])
  @@index([companyId, userId])
  @@index([companyId, candidateId])
  @@map("schedule_participants")
}

model NotificationIntent {
  id                 String                   @id @default(cuid())
  companyId          String                   @map("company_id")
  type               NotificationIntentType
  channel            NotificationChannel
  status             NotificationIntentStatus @default(PENDING)
  recipientEmail     String                   @map("recipient_email")
  recipientName      String?                  @map("recipient_name")
  targetResourceType String                   @map("target_resource_type")
  targetResourceId   String                   @map("target_resource_id")
  payloadJson        Json                     @map("payload_json")
  scheduledFor       DateTime?                @map("scheduled_for")
  dispatchedAt       DateTime?                @map("dispatched_at")
  cancelledAt        DateTime?                @map("cancelled_at")
  failureReason      String?                  @map("failure_reason")
  createdAt          DateTime                 @default(now()) @map("created_at")
  updatedAt          DateTime                 @updatedAt @map("updated_at")

  company Company @relation(fields: [companyId], references: [id], onDelete: Restrict)
  @@unique([companyId, id])
  @@index([companyId, status, scheduledFor])
  @@index([companyId, targetResourceType, targetResourceId])
  @@map("notification_intents")
}

model EmailSettings {
  id                   String    @id @default(cuid())
  companyId            String    @unique @map("company_id")
  defaultSmtpProfileId String?   @map("default_smtp_profile_id")
  tenantEmailDisabledAt DateTime? @map("tenant_email_disabled_at")
  disabledReason       String?   @map("disabled_reason")
  metadataJson         Json      @map("metadata_json")
  createdAt            DateTime  @default(now()) @map("created_at")
  updatedAt            DateTime  @updatedAt @map("updated_at")

  company Company @relation(fields: [companyId], references: [id], onDelete: Restrict)

  @@unique([companyId, id])
  @@map("email_settings")
}

model SmtpProfile {
  id                       String             @id @default(cuid())
  companyId                String?            @map("company_id")
  provider                 EmailProvider      @default(SMTP)
  name                     String
  host                     String
  port                     Int
  secure                   Boolean            @default(true)
  fromName                 String             @map("from_name")
  fromEmail                String             @map("from_email")
  normalizedFromEmail      String             @map("normalized_from_email")
  replyToEmail             String?            @map("reply_to_email")
  normalizedReplyToEmail   String?            @map("normalized_reply_to_email")
  secretRef                String             @map("secret_ref")
  status                   SmtpProfileStatus  @default(ACTIVE)
  domainVerificationStatus SenderDomainStatus @default(PENDING) @map("domain_verification_status")
  createdAt                DateTime           @default(now()) @map("created_at")
  updatedAt                DateTime           @updatedAt @map("updated_at")
  archivedAt               DateTime?          @map("archived_at")

  company Company? @relation(fields: [companyId], references: [id], onDelete: Restrict)

  @@unique([companyId, id])
  @@unique([companyId, name])
  @@index([companyId, status])
  @@index([companyId, normalizedFromEmail])
  @@map("smtp_profiles")
}

model VerifiedSenderDomain {
  id                    String             @id @default(cuid())
  companyId             String             @map("company_id")
  domain                String
  normalizedDomain      String             @map("normalized_domain")
  status                SenderDomainStatus @default(PENDING)
  verificationTokenHash String             @map("verification_token_hash")
  dnsTxtName            String             @map("dns_txt_name")
  dnsTxtValue           String             @map("dns_txt_value")
  failureReason         String?            @map("failure_reason")
  verifiedAt            DateTime?          @map("verified_at")
  createdAt             DateTime           @default(now()) @map("created_at")
  updatedAt             DateTime           @updatedAt @map("updated_at")

  company Company @relation(fields: [companyId], references: [id], onDelete: Restrict)

  @@unique([companyId, id])
  @@unique([companyId, normalizedDomain])
  @@index([companyId, status])
  @@map("verified_sender_domains")
}

model EmailTemplate {
  id            String              @id @default(cuid())
  companyId     String?             @map("company_id")
  key           EmailTemplateKey
  name          String
  subject       String
  htmlBody      String              @map("html_body")
  textBody      String              @map("text_body")
  schemaVersion Int                 @map("schema_version")
  version       Int
  status        EmailTemplateStatus @default(DRAFT)
  createdAt     DateTime            @default(now()) @map("created_at")
  updatedAt     DateTime            @updatedAt @map("updated_at")
  publishedAt   DateTime?           @map("published_at")
  archivedAt    DateTime?           @map("archived_at")

  company Company? @relation(fields: [companyId], references: [id], onDelete: Restrict)

  @@unique([companyId, id])
  @@unique([companyId, key, version])
  @@index([companyId, key, status])
  @@map("email_templates")
}

model EmailDelivery {
  id                       String              @id @default(cuid())
  companyId                String              @map("company_id")
  notificationIntentId     String?             @map("notification_intent_id")
  templateId               String?             @map("template_id")
  templateKey              EmailTemplateKey    @map("template_key")
  templateVersion          Int?                @map("template_version")
  smtpProfileId            String?             @map("smtp_profile_id")
  recipientEmail           String              @map("recipient_email")
  normalizedRecipientEmail String              @map("normalized_recipient_email")
  recipientName            String?             @map("recipient_name")
  subject                  String
  status                   EmailDeliveryStatus @default(PENDING)
  idempotencyKey           String?             @map("idempotency_key")
  provider                 EmailProvider
  providerMessageId        String?             @map("provider_message_id")
  queuedAt                 DateTime?           @map("queued_at")
  sendingAt                DateTime?           @map("sending_at")
  sentAt                   DateTime?           @map("sent_at")
  deliveredAt              DateTime?           @map("delivered_at")
  deferredAt               DateTime?           @map("deferred_at")
  bouncedAt                DateTime?           @map("bounced_at")
  complainedAt             DateTime?           @map("complained_at")
  failedAt                 DateTime?           @map("failed_at")
  cancelledAt              DateTime?           @map("cancelled_at")
  errorCode                String?             @map("error_code")
  errorMessage             String?             @map("error_message")
  metadataJson             Json                @map("metadata_json")
  createdAt                DateTime            @default(now()) @map("created_at")
  updatedAt                DateTime            @updatedAt @map("updated_at")

  company            Company             @relation(fields: [companyId], references: [id], onDelete: Restrict)
  attempts           EmailDeliveryAttempt[]
  events             EmailEvent[]

  @@unique([companyId, id])
  @@unique([companyId, idempotencyKey])
  @@index([companyId, status, createdAt])
  @@index([companyId, normalizedRecipientEmail])
  @@index([companyId, notificationIntentId])
  @@map("email_deliveries")
}

model EmailDeliveryAttempt {
  id                String                     @id @default(cuid())
  companyId         String                     @map("company_id")
  deliveryId        String                     @map("delivery_id")
  attemptNumber     Int                        @map("attempt_number")
  status            EmailDeliveryAttemptStatus
  provider          EmailProvider
  providerMessageId String?                    @map("provider_message_id")
  errorCode         String?                    @map("error_code")
  errorMessage      String?                    @map("error_message")
  startedAt         DateTime                   @map("started_at")
  completedAt       DateTime?                  @map("completed_at")
  metadataJson      Json                       @map("metadata_json")
  createdAt         DateTime                   @default(now()) @map("created_at")

  company  Company       @relation(fields: [companyId], references: [id], onDelete: Restrict)
  delivery EmailDelivery @relation(fields: [companyId, deliveryId], references: [companyId, id], onDelete: Cascade)

  @@unique([companyId, id])
  @@unique([companyId, deliveryId, attemptNumber])
  @@index([companyId, deliveryId])
  @@index([companyId, status, startedAt])
  @@map("email_delivery_attempts")
}

model EmailEvent {
  id                String         @id @default(cuid())
  companyId         String         @map("company_id")
  deliveryId        String         @map("delivery_id")
  type              EmailEventType
  provider          EmailProvider
  providerMessageId String?        @map("provider_message_id")
  reasonCode        String?        @map("reason_code")
  reasonText        String?        @map("reason_text")
  occurredAt        DateTime       @map("occurred_at")
  metadataJson      Json           @map("metadata_json")
  createdAt         DateTime       @default(now()) @map("created_at")

  company  Company       @relation(fields: [companyId], references: [id], onDelete: Restrict)
  delivery EmailDelivery @relation(fields: [companyId, deliveryId], references: [companyId, id], onDelete: Cascade)

  @@unique([companyId, id])
  @@index([companyId, deliveryId, occurredAt])
  @@index([companyId, type, occurredAt])
  @@map("email_events")
}

model ProcessingWorkflow {
  id               String         @id @default(cuid())
  companyId        String         @map("company_id")
  workflowType     String         @map("workflow_type")
  subjectType      String         @map("subject_type")
  subjectId        String         @map("subject_id")
  status           WorkflowStatus @default(PENDING)
  idempotencyKey   String         @map("idempotency_key")
  currentStepKey   String?        @map("current_step_key")
  requestId        String?        @map("request_id")
  correlationId    String?        @map("correlation_id")
  startedAt        DateTime?      @map("started_at")
  completedAt      DateTime?      @map("completed_at")
  failedAt         DateTime?      @map("failed_at")
  cancelledAt      DateTime?      @map("cancelled_at")
  failureKind      WorkflowFailureKind? @map("failure_kind")
  failureCode      String?        @map("failure_code")
  failureMessage   String?        @map("failure_message")
  checkpointJson   Json           @map("checkpoint_json")
  metadataJson     Json           @map("metadata_json")
  createdAt        DateTime       @default(now()) @map("created_at")
  updatedAt        DateTime       @updatedAt @map("updated_at")

  company Company                  @relation(fields: [companyId], references: [id], onDelete: Restrict)
  steps   ProcessingWorkflowStep[]

  @@unique([companyId, id])
  @@unique([companyId, idempotencyKey])
  @@index([companyId, status, createdAt])
  @@index([companyId, subjectType, subjectId])
  @@index([companyId, workflowType, status])
  @@map("processing_workflows")
}

model ProcessingWorkflowStep {
  id                  String             @id @default(cuid())
  companyId           String             @map("company_id")
  workflowId          String             @map("workflow_id")
  stepKey             String             @map("step_key")
  queueName           String             @map("queue_name")
  status              WorkflowStepStatus @default(PENDING)
  sequence            Int
  idempotencyKey      String             @map("idempotency_key")
  dependencyStepKeys  String[]           @map("dependency_step_keys")
  attemptCount        Int                @default(0) @map("attempt_count")
  maxAttempts         Int                @default(3) @map("max_attempts")
  nextRunAt           DateTime?          @map("next_run_at")
  startedAt           DateTime?          @map("started_at")
  completedAt         DateTime?          @map("completed_at")
  failedAt            DateTime?          @map("failed_at")
  cancelledAt         DateTime?          @map("cancelled_at")
  failureKind         WorkflowFailureKind? @map("failure_kind")
  failureCode         String?            @map("failure_code")
  failureMessage      String?            @map("failure_message")
  checkpointJson      Json               @map("checkpoint_json")
  metadataJson        Json               @map("metadata_json")
  createdAt           DateTime           @default(now()) @map("created_at")
  updatedAt           DateTime           @updatedAt @map("updated_at")

  company  Company                 @relation(fields: [companyId], references: [id], onDelete: Restrict)
  workflow ProcessingWorkflow      @relation(fields: [companyId, workflowId], references: [companyId, id], onDelete: Cascade)
  attempts WorkflowStepAttempt[]
  deadLetters WorkflowDeadLetterJob[]

  @@unique([companyId, id])
  @@unique([companyId, idempotencyKey])
  @@unique([companyId, workflowId, stepKey])
  @@index([companyId, workflowId, sequence])
  @@index([companyId, status, nextRunAt])
  @@index([companyId, queueName, status])
  @@map("processing_workflow_steps")
}

model WorkflowStepAttempt {
  id             String               @id @default(cuid())
  companyId      String               @map("company_id")
  workflowId     String               @map("workflow_id")
  stepId         String               @map("step_id")
  attemptNumber  Int                  @map("attempt_number")
  status         WorkflowStepStatus
  failureKind    WorkflowFailureKind? @map("failure_kind")
  errorCode      String?              @map("error_code")
  errorMessage   String?              @map("error_message")
  startedAt      DateTime             @map("started_at")
  completedAt    DateTime?            @map("completed_at")
  checkpointJson Json                 @map("checkpoint_json")
  metadataJson   Json                 @map("metadata_json")
  createdAt      DateTime             @default(now()) @map("created_at")

  company Company                @relation(fields: [companyId], references: [id], onDelete: Restrict)
  step    ProcessingWorkflowStep @relation(fields: [companyId, stepId], references: [companyId, id], onDelete: Cascade)

  @@unique([companyId, id])
  @@unique([companyId, stepId, attemptNumber])
  @@index([companyId, workflowId, createdAt])
  @@index([companyId, status, createdAt])
  @@map("workflow_step_attempts")
}

model WorkflowDeadLetterJob {
  id              String                   @id @default(cuid())
  companyId       String                   @map("company_id")
  queueName       String                   @map("queue_name")
  jobName         String                   @map("job_name")
  bullJobId       String?                  @map("bull_job_id")
  workflowId      String?                  @map("workflow_id")
  stepId          String?                  @map("step_id")
  status          WorkflowDeadLetterStatus @default(OPEN)
  failureKind     WorkflowFailureKind      @map("failure_kind")
  errorCode       String?                  @map("error_code")
  errorMessage    String?                  @map("error_message")
  payloadSummaryJson Json                  @map("payload_summary_json")
  requestId       String?                  @map("request_id")
  correlationId   String?                  @map("correlation_id")
  replayedAt      DateTime?                @map("replayed_at")
  cancelledAt     DateTime?                @map("cancelled_at")
  resolvedAt      DateTime?                @map("resolved_at")
  createdAt       DateTime                 @default(now()) @map("created_at")
  updatedAt       DateTime                 @updatedAt @map("updated_at")

  company Company @relation(fields: [companyId], references: [id], onDelete: Restrict)
  step    ProcessingWorkflowStep? @relation(fields: [companyId, stepId], references: [companyId, id], onDelete: Restrict)

  @@unique([companyId, id])
  @@index([companyId, status, createdAt])
  @@index([companyId, queueName, status])
  @@index([companyId, workflowId])
  @@map("workflow_dead_letter_jobs")
}

model MediaObject {
  id                     String                @id @default(cuid())
  companyId              String                @map("company_id")
  ownerType              MediaOwnerType        @map("owner_type")
  ownerId                String                @map("owner_id")
  subjectType            MediaOwnerType        @map("subject_type")
  subjectId              String                @map("subject_id")
  purpose                MediaPurpose
  storageProvider        String                @map("storage_provider")
  bucket                 String
  region                 String?
  storageKey             String                @map("storage_key")
  mimeType               String                @map("mime_type")
  sizeBytes              BigInt?               @map("size_bytes")
  expectedSizeBytes      BigInt?               @map("expected_size_bytes")
  checksumSha256         String?               @map("checksum_sha256")
  expectedChecksumSha256 String?               @map("expected_checksum_sha256")
  uploadStatus           MediaUploadStatus     @default(PENDING) @map("upload_status")
  processingStatus       MediaProcessingStatus @default(PENDING) @map("processing_status")
  retentionClass         MediaRetentionClass   @map("retention_class")
  encryptionRef          String?               @map("encryption_ref")
  retentionDeleteAt      DateTime              @map("retention_delete_at")
  legalHoldId            String?               @map("legal_hold_id")
  legalHoldActive        Boolean               @default(false) @map("legal_hold_active")
  completedAt            DateTime?             @map("completed_at")
  deletedAt              DateTime?             @map("deleted_at")
  deletionRequestedAt    DateTime?             @map("deletion_requested_at")
  providerMetadataJson   Json                  @map("provider_metadata_json")
  metadataJson           Json                  @map("metadata_json")
  createdAt              DateTime              @default(now()) @map("created_at")
  updatedAt              DateTime              @updatedAt @map("updated_at")

  company Company @relation(fields: [companyId], references: [id], onDelete: Restrict)
  uploadSessions MediaUploadSession[]
  interviewTurnMedia InterviewTurnMedia[]

  @@unique([companyId, id])
  @@unique([companyId, storageKey])
  @@index([companyId, ownerType, ownerId])
  @@index([companyId, subjectType, subjectId])
  @@index([companyId, purpose, uploadStatus])
  @@index([companyId, processingStatus, createdAt])
  @@index([companyId, retentionDeleteAt])
  @@map("media_objects")
}

model MediaUploadSession {
  id                    String            @id @default(cuid())
  companyId             String            @map("company_id")
  mediaObjectId         String            @map("media_object_id")
  kind                  MediaUploadKind
  status                MediaUploadStatus @default(AUTHORIZED)
  providerUploadId      String?           @map("provider_upload_id")
  idempotencyKey        String            @map("idempotency_key")
  partSizeBytes         Int?              @map("part_size_bytes")
  expectedPartCount     Int?              @map("expected_part_count")
  expiresAt             DateTime          @map("expires_at")
  completedAt           DateTime?         @map("completed_at")
  abortedAt             DateTime?         @map("aborted_at")
  requestId             String?           @map("request_id")
  correlationId         String?           @map("correlation_id")
  providerMetadataJson  Json              @map("provider_metadata_json")
  metadataJson          Json              @map("metadata_json")
  createdAt             DateTime          @default(now()) @map("created_at")
  updatedAt             DateTime          @updatedAt @map("updated_at")

  company Company @relation(fields: [companyId], references: [id], onDelete: Restrict)
  mediaObject MediaObject @relation(fields: [companyId, mediaObjectId], references: [companyId, id], onDelete: Cascade)
  parts MediaUploadPart[]

  @@unique([companyId, id])
  @@unique([companyId, idempotencyKey])
  @@index([companyId, mediaObjectId, status])
  @@index([companyId, status, expiresAt])
  @@map("media_upload_sessions")
}

model MediaUploadPart {
  id              String   @id @default(cuid())
  companyId       String   @map("company_id")
  uploadSessionId String   @map("upload_session_id")
  mediaObjectId   String   @map("media_object_id")
  partNumber      Int      @map("part_number")
  etag            String?  @map("etag")
  checksumSha256  String?  @map("checksum_sha256")
  sizeBytes       BigInt?  @map("size_bytes")
  uploadedAt      DateTime? @map("uploaded_at")
  metadataJson    Json     @map("metadata_json")
  createdAt       DateTime @default(now()) @map("created_at")
  updatedAt       DateTime @updatedAt @map("updated_at")

  company Company @relation(fields: [companyId], references: [id], onDelete: Restrict)
  uploadSession MediaUploadSession @relation(fields: [companyId, uploadSessionId], references: [companyId, id], onDelete: Cascade)

  @@unique([companyId, id])
  @@unique([companyId, uploadSessionId, partNumber])
  @@index([companyId, mediaObjectId])
  @@map("media_upload_parts")
}
